import java.util.Scanner;

public class Dice
{
public static int Throw()
{
int randomNumber = 1+(int)(Math.random()*6);
return randomNumber;
}

public static int Throw(int dices, int bounces)
{
int randomNumber = (dices*(1+(int)(Math.random()*6)))+bounces;
return randomNumber;
}
}
